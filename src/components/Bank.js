import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Bank = () => {
  return (
    <View>
      <Text>Bank</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Bank;
